package nv.tests

import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Assertions.assertEquals

/**
 * Placeholder test — Phase 0 scaffolding.
 *
 * Phase 1 will replace this with golden-file tests (each .nv in tests/golden/
 * has a paired .expected file matched against `nv run` output) and parse tests
 * (AST output compared to serialized .ast.expected files).
 */
class PlaceholderTest {

    @Test
    fun `scaffolding compiles and tests run`() {
        assertEquals(1 + 1, 2)
    }
}
